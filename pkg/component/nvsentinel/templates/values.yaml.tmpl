# NVSentinel default values
# Note: Header metadata is added during bundle generation
# Node selector and tolerations can be configured via CLI flags:
#   --system-node-selector key=value                # Sets global.systemNodeSelector
#   --accelerated-node-toleration key=value:effect  # Sets global.tolerations

global: {}